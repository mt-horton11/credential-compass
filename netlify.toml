[build]
  command = "npm install && npm run build"
  publish = "dist"

[functions]
  included_files = ["netlify/functions/**"]

[[headers]]
  for = "/api/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
